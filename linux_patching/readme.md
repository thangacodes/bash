# Script functionality

```bash
SSHes into each server listed in servers.txt.

Verifies the hostname and IP.

Checks for BIND9 installation.

Verifies it is the expected version (9.16.50-Debian).

If not, upgrades it.
